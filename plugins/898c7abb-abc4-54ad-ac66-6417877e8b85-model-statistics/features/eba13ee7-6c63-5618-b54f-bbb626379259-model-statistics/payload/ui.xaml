<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="模型统计"
        Height="780"
        Width="1180"
        MinHeight="680"
        MinWidth="960"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA"
        FontFamily="Microsoft YaHei UI">
    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="TextBrush" Color="#1F2937"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#D7E0EA"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1D4ED8"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="RowBackground" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Extended"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="110"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="模型统计" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="按指定族或类型查看族实例数量，统计结果仅供查看，不修改模型。"
                           Margin="0,5,0,0"
                           Foreground="{DynamicResource MutedTextBrush}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="刷新统计" Click="RefreshOverview_Click" ToolTip="重新读取当前 Revit 模型并刷新总览和结果。"/>
                <Button Content="导出图纸名称" Click="ExportSheets_Click" ToolTip="选择目录和文件名，将当前模型全部图纸的编号与名称导出为 XLSX。"/>
                <Button Content="关闭" Click="Close_Click" ToolTip="关闭模型统计窗口。" Margin="0"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="14"
                Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="族类别" Foreground="{DynamicResource MutedTextBrush}"/>
                    <TextBlock x:Name="FamilyCategoryCountTextBlock" Text="0" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="族实例总数" Foreground="{DynamicResource MutedTextBrush}"/>
                    <TextBlock x:Name="FamilyInstanceCountTextBlock" Text="0" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="电缆桥架" Foreground="{DynamicResource MutedTextBrush}"/>
                    <TextBlock x:Name="CableTrayCountTextBlock" Text="0" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="3">
                    <TextBlock Text="图纸" Foreground="{DynamicResource MutedTextBrush}"/>
                    <TextBlock x:Name="SheetCountTextBlock" Text="0" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/>
                    <TextBlock x:Name="OverviewDetailTextBlock" Text="电缆桥架类型 0 个 | 总长度 0.00 m"
                               Foreground="{DynamicResource MutedTextBrush}"
                               FontSize="11"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="14"
                Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*"/>
                    <ColumnDefinition Width="1.7*"/>
                    <ColumnDefinition Width="1.7*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="统计范围"/>
                    <ComboBox x:Name="ScopeComboBox" SelectionChanged="ScopeComboBox_SelectionChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="指定族"/>
                    <ComboBox x:Name="FamilyComboBox" SelectionChanged="FamilyComboBox_SelectionChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Text="指定类型"/>
                    <ComboBox x:Name="TypeComboBox" SelectionChanged="TypeComboBox_SelectionChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="扫描范围" Click="Scan_Click" ToolTip="按当前统计范围重新扫描族实例。" Margin="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="14"
                Margin="0,0,0,14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="族实例数量结果" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,18,0">
                        <TextBlock Text="匹配实例 " Foreground="{DynamicResource MutedTextBrush}"/>
                        <TextBlock x:Name="SelectedCountTextBlock" Text="0" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <TextBlock Text="组合数 " Foreground="{DynamicResource MutedTextBrush}"/>
                        <TextBlock x:Name="ResultGroupCountTextBlock" Text="0" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
                <TextBlock Grid.Row="1"
                           x:Name="FilterSummaryTextBlock"
                           Text="范围：全模型 | 族：全部 | 类型：全部"
                           Foreground="{DynamicResource MutedTextBrush}"
                           Margin="0,0,0,10"/>
                <DataGrid x:Name="ResultsDataGrid" Grid.Row="2" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="类别" Binding="{Binding Category}" Width="150"/>
                        <DataGridTextColumn Header="族名" Binding="{Binding FamilyName}" Width="220"/>
                        <DataGridTextColumn Header="类型名" Binding="{Binding TypeName}" Width="220"/>
                        <DataGridTextColumn Header="实例数量" Binding="{Binding InstanceCount}" Width="110"/>
                        <DataGridTextColumn Header="实例 ID" Binding="{Binding ElementIds}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="4"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="状态与日志" FontWeight="Bold" Margin="0,0,0,6"/>
                <TextBox x:Name="LogTextBox" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>

        <TextBlock x:Name="StatusTextBlock" Grid.Row="4" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,4,18,0" Foreground="{DynamicResource MutedTextBrush}"/>
    </Grid>
</Window>
